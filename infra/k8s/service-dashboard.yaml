apiVersion: v1
kind: Service
metadata:
  name: meldit-dashboard
  namespace: meldit
spec:
  selector:
    app: meldit-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8501
  type: ClusterIP


